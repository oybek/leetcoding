package io.github.oybek.leetcode.BeautifulArrangement

object Solution {
  def countArrangement(n: Int): Int = {
    0
  }

  def solve(i: Int, nums: List[Int]): Int = {
    if (nums.isEmpty) 1
    else {
      0
    }
  }
}
